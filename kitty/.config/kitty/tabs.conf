# Tab 栏配置

# Tab 栏样式 (fade, slant, separator, powerline, custom, hidden)
tab_bar_style powerline

# Tab 栏位置 (top, bottom)
tab_bar_edge top

# Tab 标题模板
tab_title_template "{index}: {title}"

# 激活的 tab 字体样式
active_tab_font_style bold

# Tab 栏最小标签数（少于这个数量时隐藏 tab 栏）
tab_bar_min_tabs 2
