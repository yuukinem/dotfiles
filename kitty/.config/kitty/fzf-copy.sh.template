#!/bin/bash
# Kitty fzf search and copy script
# Generated from template - DO NOT EDIT DIRECTLY
# Edit fzf-copy.sh.template instead and run install.sh

# Multi-select with fzf, strip shell prompts, copy to clipboard
__FZF__ --multi --no-sort --no-mouse --exact -i | \
    sed 's/^[❯›➜>$#] *//' | \
    sed 's/^.*@.*:.*[❯›➜>$#] *//' | \
    perl -pe 'chomp if eof' | \
    __COPY__
