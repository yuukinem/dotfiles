# 性能优化配置

# 重绘延迟（毫秒）- 越小越流畅但可能增加 CPU 使用
repaint_delay 10

# 输入延迟（毫秒）
input_delay 3

# 同步到显示器刷新率
sync_to_monitor yes

# 调整窗口大小时的绘制策略（减少闪烁）
resize_draw_strategy static

# 调整大小的去抖动时间（秒）
resize_debounce_time 0.1
